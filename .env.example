# ── HackoBlast environment variables ──────────────
PORT=5000
MONGO_URI=mongodb://localhost:27017/hackoblast

# ⚠️  WARNING: You MUST replace the placeholder below with a strong, random secret
# before deploying. Generate one with:
#   openssl rand -base64 32
# NEVER commit your real secret to version control or share it publicly.
# Store production secrets in a secure vault or environment-specific config.
JWT_SECRET=change_me_to_a_strong_secret

# Google Gemini API key — get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
